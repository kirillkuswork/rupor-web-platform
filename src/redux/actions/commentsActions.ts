import {
  deleteComments,
  setDeletingComment,
  setCommentReplies,
  setComments,
} from '../slices/commentsSlice';

export const commentsActions = {
  setCommentReplies,
  setComments,
  deleteComments,
  setDeletingComment,
};
